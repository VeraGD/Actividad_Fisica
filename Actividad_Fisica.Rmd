---
title: "Actividad física, zonas verdes y salud mental"
author: "Vera García Díez y Sara González Bárcena"
date: '`r format(Sys.Date(), "%d %B %Y")`'
output: html_document
---





## Objetivo General
Analizar si existe alguna relación entre la realización de actividad física a mayor zonas verdes y la salud mental de las personas.

Los datos han sido obtenidos del *Instituto Nacional de Estadística (INE)*.

- **Satisfacción con las zonas verdes por CCAA y nivel de satisfacción **
[satisfaccion_zonas_verdes](https://www.ine.es/jaxi/Datos.htm?path=/t00/ICV/dim8/&file=82101.px)

- **Enfermedades crónicas**
[enfermedades_cronicas](https://www.ine.es/jaxiT3/Datos.htm?tpx=47669)

- **Número de días por semana de ejercicio físico según sexo y cCCAA. Población de 15 y más años**
[ejercicio_fisico](https://www.ine.es/jaxiT3/Datos.htm?tpx=47605)

```{r include=FALSE}
library(readxl)
library(dplyr)
library(tidyverse)
library(DT)
```

### Tabla modificada satisfacción con las zonas verdes por CCA y nivel de satisfacción.

```{r include=FALSE}
zonasVerdes <- read_excel("INPUT/DATA/satisf_ZV.xlsx", 
                          range = "A7:F27")
```

```{r echo=TRUE}
ZV <- zonasVerdes %>% 
  slice(2:20) %>% 
  rename(comunidades = ...1, Valoración = `Valoración media`)

```

```{r echo=FALSE, message=FALSE, warning=FALSE}
DT::datatable(ZV)
```

### Tabla modificada enfermedades crónicas, ansiedad y depresión, por CCAA y nº de personas.

```{r include=FALSE}
saludMental <- read_excel("INPUT/DATA/s_mental.xlsx", 
                          range = "A7:CS71")
```

```{r echo=TRUE}
SM <- saludMental %>% 
  rename(Comunidades = ...1, depresión = ...60, ansiedad = ...63) %>% 
  pivot_longer(names_to = "Enfermedades", values_to = "Personas", cols = c(depresión, ansiedad)) %>%
  slice(c(7:44)) %>% 
  select(c(1,96,97))

```

```{r echo=FALSE, message=FALSE, warning=FALSE}
DT::datatable(SM)
```


### Tabla modificada al número de días promedio a la semana por CCAA.
```{r include=FALSE}
actFisica <- read_excel("INPUT/DATA/Act_Fisica.xlsx", 
                        range = "A7:H70")
```

```{r echo=TRUE}
AF <-
actFisica %>% 
  slice(3:21) %>% 
  rename(Comunidades = ...1, d1_2 = `1 o 2 días a la semana`, 
         d3_4 = `3 o 4 días a la semana`,
         d5_6 = `5 o 6 días a la semana`,
         d7 = `7 días a la semana`) %>% 
  select(c(1,2,4:7)) %>% 
  pivot_longer(names_to = "Frecuencia", values_to = "Días", cols = c(`d1_2`:`d7`)) %>% 
  group_by(Comunidades) %>% 
  summarise(.data = ., dias_prom = mean(Días, na.rm = TRUE))

```


```{r echo=FALSE, message=FALSE, warning=FALSE}
DT::datatable(AF)
```




## Objetivos específicos

### 1. Relación actividad física y zonas verdes
Análisis sobre a mayor porcentaje de áreas verdes, más personas realizan actividad física.


```{r echo=TRUE}
AF_ZV <-  
  AF %>% 
  select(Comunidades, dias_prom) %>% 
  full_join(x = ., 
            y = ZV %>% 
              select(comunidades, Valoración),
            by = c("Comunidades" = "comunidades"))
```

```{r echo=FALSE, message=FALSE, warning=FALSE}
DT::datatable(AF_ZV)
```


```{r echo=TRUE}
grafico1 <- AF_ZV %>% 
  filter(Valoración > 4) %>% 
  ggplot(data = ., aes(x = Valoración, y = dias_prom)) +
    geom_point(aes(colour = factor(Comunidades))) +
    geom_smooth() +
    theme_bw()
```

```{r echo=FALSE, message=FALSE, warning=FALSE}
plot(grafico1)
```


### 2. Relación actividad física y salud mental
Análisis sobre a mayor frecuencia de actvidad física, disminuye la población que padece depresión y ansiedad.


```{r echo=TRUE}
AF_SM <-  
  AF %>% 
  select(Comunidades, dias_prom) %>% 
  full_join(x = ., 
            y = SM %>% 
              select(Comunidades, Enfermedades, Personas),
            by = "Comunidades")
```

```{r echo=FALSE, message=FALSE, warning=FALSE}
DT::datatable(AF_SM)
```

```{r echo=TRUE}
grafico2 <- AF_SM %>% 
  ggplot(data = ., aes(x = Personas, y = dias_prom)) +
  geom_point(aes(colour = factor(Comunidades))) +
  geom_smooth() +
  theme_bw()
```

```{r echo=FALSE, message=FALSE, warning=FALSE}
plot(grafico2)
```



### 3. Relación salud mental y zonas verdes
Análisis sobre si disminuye las personas con ansiedad o depresión a mayor zonas verdes.
```{r echo=TRUE}
ZV_SM <-  
  ZV %>% 
  select(comunidades, Valoración) %>% 
  full_join(x = ., 
            y = SM %>% 
              select(Comunidades, Enfermedades, Personas),
            by = c("comunidades" = "Comunidades"))


```

```{r echo=FALSE, message=FALSE, warning=FALSE}
DT::datatable(ZV_SM)
```

```{r echo=TRUE}
grafico3 <- ZV_SM %>% 
  filter(Valoración > 4) %>% 
  ggplot(data = ., aes(x = Personas, y = Valoración)) +
  geom_point(aes(colour = factor(comunidades))) +
  geom_smooth() +
  theme_bw()

```

```{r echo=FALSE, message=FALSE, warning=FALSE}
plot(grafico3)
```

